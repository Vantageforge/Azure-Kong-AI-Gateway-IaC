# certs/

Place the TLS certificate and key that Kong Konnect generates for your
Data Plane node (Gateway Manager → New Gateway → Self-Managed Hybrid → Kubernetes)
here as:

- `tls.crt`
- `tls.key`

Both files are gitignored — never commit them to version control.
